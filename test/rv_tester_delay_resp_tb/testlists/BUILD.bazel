# Placeholder. The previous sh_test that drove
# //test/rv_tester_delay_resp_tb:rv_tester_delay_resp_tb_verilator was removed
# because the delay-resp testbench top (rv_tester_delay_resp_tb_top.sv) uses
# bare `#100`-style delays which Verilator only honours under --timing. The
# cc_binary stays in the parent package for manual invocation.
#
# TODO(open-source): re-add the sh_test once either (a) `timing = True` is
# enabled on the parent verilator_cc_library (and the runtime cost is
# accepted), or (b) the testbench delays are rewritten to use the
# clk_int_div helper used elsewhere in rv_tester.
