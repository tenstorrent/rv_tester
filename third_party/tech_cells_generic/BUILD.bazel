load("@rules_hdl//verilog:providers.bzl", "verilog_library")

# Vendored from pulp-platform/tech_cells_generic (Solderpad 0.51). Only
# clk_mux_glitch_free is needed by rv_tester.sv; the rest of the
# tech_cells_generic library isn't on the smoke path.
# TODO(open-source): swap for the upstream pulp-platform/tech_cells_generic
# github mirror once an open-source equivalent is wired into MODULE.bazel.
verilog_library(
    name = "clk_mux_glitch_free",
    srcs = ["clk_mux_glitch_free.sv"],
    visibility = ["//visibility:public"],
)
