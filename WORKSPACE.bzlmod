# WORKSPACE.bzlmod - loaded after MODULE.bazel for hybrid mode.
# All external repo wiring now lives in MODULE.bazel; this file is intentionally
# minimal (mirrors cvm's stub).

workspace(name = "rv_tester")
